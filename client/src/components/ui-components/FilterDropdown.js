// import { Dropdown } from "flowbite-react";

// const FilterDropdown = ({ label, options, onSelect }) => {
//     return (
//         <Dropdown label={label} className="bg-red-500">
//             {options.map((option, index) => (
//                 <Dropdown.Item key={index} onClick={() => onSelect(option)} className="bg-gray-500">
//                     {option}
//                 </Dropdown.Item>
//             ))}
//         </Dropdown>
//     );
// };

// export default FilterDropdown;